println(3 100)
println(i::6 (i + 1))

void Num(int num) {
  println("num = " + num)
}

Num[] nums = x::4 new Num(x * 2)

println("add one element to array")
nums++

for (int i = 0; i < nums.size(); i++)
  println(nums[i].num)
