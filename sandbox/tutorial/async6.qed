String Wrapper(int id, int millis) {
  Timer(millis)
  return "Timer " + id + ": " +
     /$Math.floor(millis)$/ + "ms."
}

String GetFirstResult() {
  n::10 new Wrapper(n + 1, (int) (rand() * 5000)) -> return _ret
}

println(GetFirstResult() + " Done!")